@model StudentManagement.Models.StudentModel

@{
    ViewData["Title"] = "Add Student Info";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-primary text-white rounded-top-4">
                    <h4 class="mb-0">
                        <i class="bi bi-plus-circle me-2"></i> Add Student Info
                    </h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label fw-semibold"></label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CourseTitle" class="form-label fw-semibold"></label>
                            <input asp-for="CourseTitle" class="form-control" />
                            <span asp-validation-for="CourseTitle" class="text-danger small"></span>
                        </div>

                        <div class="form-group mb-4">
                            <label asp-for="TotalMarks" class="form-label fw-semibold"></label>
                            <input asp-for="TotalMarks" class="form-control" type="number" />
                            <span asp-validation-for="TotalMarks" class="text-danger small"></span>
                        </div>

                        @* <div class="form-group mb-3"> *@
                        @*     <label asp-for="Status" class="form-label fw-semibold"></label> *@
                        @*     <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<Status>()"></select> *@
                        @*     <span asp-validation-for="Status" class="text-danger small"></span> *@
                        @* </div> *@

                        <div class="d-flex justify-content-between">
                            <input type="submit" value="Create" class="btn btn-primary fw-semibold" />
                            <a asp-action="Index" class="btn btn-secondary fw-semibold">
                                <i class="bi bi-arrow-left-circle me-1"></i> Back to List
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}